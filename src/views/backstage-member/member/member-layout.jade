extends ../member-layout

block side-modules
    ul.feature_nav
        each tab in backstageTops[1].sides[0].tabs
            li
                a(href=tab.link) #{tab.name}
    .main_content
        block content