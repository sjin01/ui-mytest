extends ../system-layout

block side-modules
    ul.feature_nav
        each tab in backstageTops[3].sides[0].tabs
            li
                a(href=tab.link) #{tab.name}
    .main_content
        block content